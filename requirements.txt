# Data collection (v0.1.0)
requests==2.32.3
biopython==1.83
lxml==5.1.1
trafilatura==1.8.1
beautifulsoup4==4.12.3
tenacity==8.2.3
pubmed-parser==0.5.1
pyyaml==6.0.2
tqdm==4.66.2

# PII scrubbing (v0.1.0)
presidio-analyzer==2.2.354
presidio-anonymizer==2.2.354
spacy==3.7.4

# Corpus processing (v0.1.0)
datasketch==1.9.0
ftfy==6.3.1
scikit-learn==1.8.0

# Tokenizer training (v0.1.0)
tokenizers==0.22.2
tiktoken==0.12.0

# Model training (v0.2.0)
# NOTE: PyTorch must be installed separately with --index-url flag.
# See setup.sh for correct installation sequence.
# Do NOT add torch here -- pip install -r does not support per-package index URLs.
deepspeed==0.18.6
ninja==1.13.0

# Export pipeline (v0.2.0)
transformers==5.2.0
safetensors==0.7.0

# Export (Phase 4.4)
# NOTE: gguf must be installed from lib/llama.cpp/gguf-py/ (not PyPI) to match
# the converter script version. Run: pip install lib/llama.cpp/gguf-py/
sentencepiece==0.2.1

# GPU monitoring (v0.2.0)
nvidia-ml-py==13.590.48

# System monitoring (Phase 4.5)
psutil==7.2.2
